package rinde.sim.util.cli;

import static org.mockito.Mockito.mock;

import java.util.List;

import org.junit.Test;

public class CliTest {

  @Test
  public void test() {
    final List list = mock(List.class);
    // CliMenu.builder(list)
    // .add(options);

    // CliMenu.builder(list)
    // .add(options)
  }

}
